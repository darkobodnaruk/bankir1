<%= form_for(@loan) do |f| %>
  <% if @loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% @loan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :bank_id %><br>
    <%= f.select(:bank_id, Bank.all.map{|b| [b.name, b.id]}) %>
  </div>
  <div class="field">
    <%= f.label :loan_type_id %><br>
    <%= f.select(:loan_type_id, LoanType.all.map{|b| [b.name, b.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
